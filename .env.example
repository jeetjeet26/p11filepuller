# Dropbox API Configuration
# Replace with your actual access token from https://www.dropbox.com/developers
DROPBOX_ACCESS_TOKEN=your_access_token_here

# Optional Configuration
# Uncomment and modify these values if needed
# MAX_WORKERS=3      # Number of concurrent searches (default: 3)
# TIMEOUT=600        # Search timeout in seconds per member (default: 600)
# DEBUG=False        # Enable debug logging (default: False) 